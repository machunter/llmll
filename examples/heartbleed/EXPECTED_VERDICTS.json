{
  "family": "heartbleed",
  "frozen_at": "v0.25.5",
  "frozen_date": "2026-09-23",
  "note": "Frozen VERIFY verdicts + exit codes ONLY, for the public headline examples (README + blog). The 163-function secure-channel/agent-fill/sc-channel-agentfilled.llmll is NOT frozen here: the gate lists top-level files only and has no per-case timeout (PROC-TIMEOUT-1), and that verify was reported at about 60s and 5.3 GB peak memory. The goto-fail adversarial twin is frozen in its own suite, examples/heartbleed/secure-channel/agent-fill/adversarial.",
  "cases": [
    { "file": "heartbleed-safe.llmll", "flags": [],                         "expect_exit": 0, "expect": "safe" },
    { "file": "heartbleed-bug.llmll",  "flags": ["--strict-verified-core"], "expect_exit": 1, "expect": "refuted", "localized": "heartbeat-response",
      "why": "echoes claimed_len bytes unchecked against received_len (CVE-2014-0160); copy-bytes' precondition not proven at the call site" },
    { "file": "channel.llmll",         "flags": [],                         "expect_exit": 0, "expect": "safe" },
    { "file": "slice-gate.llmll",      "flags": [],                         "expect_exit": 0, "expect": "safe" },
    { "file": "slice-gate-bug.llmll",  "flags": [],                         "expect_exit": 1, "expect": "refuted", "localized": "deliver-plaintext",
      "why": "gate-mac's contract drops delivered => MAC verified; gate-mac verifies alone and the refutation fires at the composer" }
  ]
}
