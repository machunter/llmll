{
  "llmll_version": "0.14.63",
  "schemaVersion": "0.8.0",
  "source_file": "bytes-scaled-read.scaffold.llmll",
  "_fixture_note": "Solver-catches seed (experiment 008, bytes-scaled-read). Pre-authored fill-the-hole program: the read-scaled signature (bytes[8] int -> int), its input precondition (0 <= i < 8), a sample-buffer helper (-> bytes[8] = bytes-zero) that supplies a concrete buffer to the visible test, and a NON-ADVERSARIAL visible check are written; the agent fills ONLY the body hole read-scaled-body. GRADER-GAP INVARIANT: the discriminating postcondition ((and (>= result 0) (<= result 255)) — the byte-range ceiling) is NOT in this seed and NOT mandated in problem.md; it lives only in hidden-specs/008.json and is injected onto read-scaled at grade time, then run through fixpoint (refuted = the solver caught a fill that can exceed 255 = grade A; verified = an in-range read = grade B; vacuous = non-catch = grade C). The visible check samples read-scaled on the all-zero sample buffer at a constant in-bounds index, so a wrong fill that adds a constant (e.g. (+ (bytes-get b i) 1)) still returns a small non-negative value and passes it (tests blind); only the solver, via the withheld <= 255 ceiling on the 255-boundary byte, catches the overflow. Exercised in the bytes array class (LLMLL.md §5.3.3); bytes-get carries the ground 0 <= v <= 255 range fact. This _fixture_note is the leak-strip regression probe: prepare_run.py::_strip_fixture_metadata drops every underscore-prefixed key on copytree, so it must be ABSENT from the prepared run dir's scaffold.",
  "statements": [
    {
      "kind": "def",
      "name": "sample-buffer",
      "params": [],
      "return_type": { "kind": "bytes", "length": 8 },
      "body": { "kind": "app", "fn": "bytes-zero", "args": [] }
    },
    {
      "kind": "def",
      "name": "read-scaled",
      "params": [
        { "name": "b", "param_type": { "kind": "bytes", "length": 8 } },
        { "name": "i", "param_type": { "kind": "primitive", "name": "int" } }
      ],
      "return_type": { "kind": "primitive", "name": "int" },
      "pre": {
        "kind": "op",
        "op": "and",
        "args": [
          { "kind": "op", "op": ">=", "args": [ { "kind": "var", "name": "i" }, { "kind": "lit-int", "value": 0 } ] },
          { "kind": "op", "op": "<",  "args": [ { "kind": "var", "name": "i" }, { "kind": "lit-int", "value": 8 } ] }
        ]
      },
      "body": { "kind": "hole-named", "name": "read-scaled-body" }
    },
    {
      "kind": "check",
      "label": "scaled read is non-negative on the sample buffer",
      "for_all": {
        "bindings": [ { "name": "d", "param_type": { "kind": "primitive", "name": "int" } } ],
        "body": {
          "kind": "op",
          "op": ">=",
          "args": [
            {
              "kind": "app",
              "fn": "read-scaled",
              "args": [
                { "kind": "app", "fn": "sample-buffer", "args": [] },
                { "kind": "lit-int", "value": 0 }
              ]
            },
            { "kind": "lit-int", "value": 0 }
          ]
        }
      }
    }
  ]
}
