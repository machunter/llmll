;; AX1-00 — honest baseline / control.
;; Tight contract, correct implementation. No weakening, no annotation.
(type PositiveInt (where [x: int] (> x 0)))

(def withdraw [balance: int amount: PositiveInt]
  (pre  (>= balance amount))
  (post (= result (- balance amount)))
  (- balance amount))
