{
  "_purpose": "Ad-hoc Phase-1.5 kink-finding manifest for the Python target. k=1 real-agent run using the testkits/002-bank-ledger/python/{solution,test_solution}.py stubs.",
  "experiments": ["002-bank-ledger"],
  "targets": ["python"],
  "run_count": 1,
  "repair_budget_k": 1,
  "terminal_target": {
    "kind": "all-pass",
    "value": "all-verifier-commands-pass"
  },
  "timeout_seconds_per_turn": 120,
  "agents": [
    {
      "name": "shim-python-stub",
      "cmd": "cp ../../testkits/002-bank-ledger/python/solution.py solution.py && cp ../../testkits/002-bank-ledger/python/test_solution.py test_solution.py",
      "note": "Real-agent code-path test for the Python adapter. Two cp invocations (solution + colocated test) because the orchestrator's _find_solution looks only for the priority-1 file; the test file is copied alongside as a Phase-1.5 testkit pre-injection."
    }
  ]
}
